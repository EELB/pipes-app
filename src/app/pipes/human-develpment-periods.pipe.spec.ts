import { HumanDevelpmentPeriodsPipe } from './human-develpment-periods.pipe';

describe('HumanDevelpmentPeriodsPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanDevelpmentPeriodsPipe();
    expect(pipe).toBeTruthy();
  });
});
